# Citation-Generator
Generates a valid `CITATION.cff` file directly from an existing `pyproject.toml` — the metadata (name, version, authors, license, repository) that nearly every Python research project already has, turned into a real, officially-validated citation file in one command.
